{
  "version": 2,
  "installCommand": "pnpm install && pnpm prisma generate",
  "buildCommand": "pnpm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ],
  "regions": ["sin1"]
}
